<div class="course-card">

    <div class="course-thumbnail">

        <img
            src="<%= course.thumbnailUrl || '/assets/images/course-placeholder.svg' %>"
            alt="<%= course.title %>"
            loading="lazy"
            onerror="this.onerror=null;this.src='/assets/images/course-placeholder.svg';">

        <% if (course.featured) { %>
            <span class="course-badge">Featured</span>
        <% } else if (course.level === 'ADVANCED') { %>
            <span class="course-badge">Advanced</span>
        <% } %>

    </div>

    <div class="course-content">

        <div class="course-category">
            <%= course.category ? course.category.name : 'General' %>
        </div>

        <h3 class="course-title">
            <%= course.title %>
        </h3>

        <%
            var reviewRatings = Array.isArray(course.reviews) ? course.reviews.map(function (r) { return r.rating; }) : [];
            var avgRating = reviewRatings.length > 0
                ? (reviewRatings.reduce(function (a, b) { return a + b; }, 0) / reviewRatings.length)
                : null;
        %>
        <div class="course-rating">
            <% if (avgRating) { %>
                <span class="course-stars">
                    <%= '★'.repeat(Math.round(avgRating)) %><%= '☆'.repeat(5 - Math.round(avgRating)) %>
                </span>
                <strong><%= avgRating.toFixed(1) %></strong>
                <span>(<%= reviewRatings.length %> review<%= reviewRatings.length === 1 ? '' : 's' %>)</span>
            <% } else { %>
                <span class="text-muted small">No reviews yet</span>
            <% } %>
        </div>

        <div class="course-meta">

            <span>
                <i class="bi bi-play-btn"></i>
                <%= course._count ? course._count.lessons : 0 %> Lessons
            </span>

            <span>
                <i class="bi bi-clock"></i>
                <%= course.duration || 'TBD' %>
            </span>

        </div>

        <div class="course-footer">

            <div class="course-price">
                <%= course.currency %> <%= Number(course.salePrice || course.price).toLocaleString('en-IN') %>

                <% if (course.salePrice) { %>
                    <del><%= course.currency %> <%= Number(course.price).toLocaleString('en-IN') %></del>
                <% } %>
            </div>

            <a href="/courses/<%= course.slug %>" class="course-view-btn">
                View Course
                <i class="bi bi-arrow-right"></i>
            </a>

        </div>

    </div>

</div>
